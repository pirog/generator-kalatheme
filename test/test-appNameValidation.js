/*global describe, beforeEach, it */
'use strict';

appNameValidation = require('../generators/app/appNameValidation');

describe('machine name validation', function () {
  it('returns false for strings with spaces in it', function() {
    expect()
  });
});
